apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: team-config-cluster-a
spec:
  project: default

  destination:
    namespace: argocd
    server: 'https://kubernetes.default.svc'

  source:
    path: cluster/cluster-a/teams
    repoURL: 'https://github.com/jwennerberg/argocd-openshift.git'
    targetRevision: dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    validate: true
